STORE_CORS=http://localhost:8000,https://docs.medusajs.com
ADMIN_CORS=http://localhost:5173,http://localhost:9000,https://docs.medusajs.com
AUTH_CORS=http://localhost:5173,http://localhost:9000,https://docs.medusajs.com
REDIS_URL=redis://localhost:6379
JWT_SECRET=supersecret
COOKIE_SECRET=supersecret
DATABASE_URL=
DB_NAME=medusa-v2

# Xendit Payment Provider Configuration
# Get your SECRET API key from: https://dashboard.xendit.co/settings/developers#api-keys
# Note: Use Secret Key (NOT Public Key). Secret keys start with "xnd_development_" or "xnd_production_"
# Secret keys can perform any API request and should ONLY be used server-side
XENDIT_SECRET_KEY=xnd_development_your_secret_key_here

# Webhook verification token (REQUIRED for production, optional for development)
# Get from: https://dashboard.xendit.co/settings/developers#webhooks
# This is the "Callback Token" shown when you set your webhook URL
# Used to verify webhook authenticity and prevent man-in-the-middle attacks
# Without this, webhooks are accepted but a security warning is logged
# STRONGLY RECOMMENDED by Xendit to prevent money loss incidents
XENDIT_WEBHOOK_TOKEN=your_webhook_verification_token_here

# Required: ISO 3166-1 alpha-2 country code (e.g., ID, PH, MY, TH)
XENDIT_DEFAULT_COUNTRY=ID

# Enable Test Mode (automatically enables payment simulation)
XENDIT_TEST_MODE=true
